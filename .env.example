# SQLite database file (created automatically on first run).
# For production you can switch to Postgres via @payloadcms/db-postgres.
DATABASE_URL=file:./flatirons-ai.db

# Used to encrypt Payload sessions & tokens. Generate a long random string.
PAYLOAD_SECRET=YOUR_SECRET_HERE
